rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if true;
    }

    // match /mediafiles {
    //   allow read: if true;
    //   allow write: if false;
    // }

    // match /userupload/{userId} {
    //   allow read, write: if request.auth!=null && request.auth.uid == userId;
    // }
  }
}
